{
  "name": "xusn-site",
  "version": "2.0.0",
  "private": true,
  "description": "A static personal homepage that can be deployed to Cloudflare Pages or Vercel.",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "node scripts/build.js",
    "deploy:vercel": "vercel --prod",
    "deploy:cloudflare": "wrangler pages deploy public"
  }
}
